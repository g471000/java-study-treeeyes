[6-15] 다음중 인스턴스변수의 초기화 순서가 올바른 것은?

a. 기본값-명시적초기화-초기화블럭-생성자
b. 기본값-명시적초기화-생성자-초기화블럭
c. 기본값-초기화블럭-명시적초기화-생성자
d. 기본값-초기화블럭-생성자-명시적초기화

when the instance was created,
it creates the instance variable,
then it gives default variable (0 for int, 0.0 for double etc)
then initialize block runs
then the constructor runs

so, answer is a.